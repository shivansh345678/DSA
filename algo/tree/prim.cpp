#include <iostream>
#include <cstdlib>
using namespace std;
int main()
{
    int n, m;
    scanf("%d %d", &n, &m);
    int adj[(n + 1) * (m + 1)][3];
    for (int i = 1; i <= n; i++)
    {
        for (int j = 1; j <= m; j++)
        {
            adj[i][j] = 0;
        }
    }
    for()
    return 0;
}
